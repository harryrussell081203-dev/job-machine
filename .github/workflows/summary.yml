name: job-machine summary

# Fires at 21:00 and 22:00 UTC every day. Exactly one of those is 22:00 in the
# UK whichever way the clocks are set, and the script sends only on that one.
on:
  schedule:
    - cron: "0 21 * * *"
    - cron: "0 22 * * *"
  workflow_dispatch:
    inputs:
      force:
        description: "Send the digest now, ignoring the 22:00 UK gate (true/false)"
        required: false
        default: "true"

permissions:
  contents: write

concurrency:
  # Per branch. A group holds exactly one pending run and the next push into it
  # deletes that run rather than queueing behind it - see the long note in
  # run.yml. Main's scheduled runs still serialise against each other, which is
  # the only place serialising was ever wanted.
  group: job-machine-summary-${{ github.ref }}
  cancel-in-progress: false

jobs:
  summary:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install requests dnspython

      - name: Send daily summary
        env:
          # Python block-buffers stdout when it is a pipe, which is
          # what Actions gives it. Every log in this repo was flushed
          # in one lump at exit, so a run that was killed printed
          # nothing at all - forty-five minutes of silence that read
          # like a hang and was really an empty buffer.
          PYTHONUNBUFFERED: "1"
          GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          # httpSMS: the app on Harry's own phone. The key is a repo secret
          # and lives nowhere else - this repository is public. Without it
          # every texting path is simply skipped.
          HTTPSMS_API_KEY: ${{ secrets.HTTPSMS_API_KEY }}
          SMS_FROM: ${{ vars.SMS_FROM }}
          SMS_OUTBOUND: ${{ vars.SMS_OUTBOUND }}
          DAILY_SMS_CAP: ${{ vars.DAILY_SMS_CAP }}
        run: |
          if [ "${{ github.event.inputs.force }}" = "true" ]; then
            python job_machine.py --summary --force
          else
            python job_machine.py --summary
          fi

      - name: Commit state
        if: always()
        env:
          TARGET: ${{ startsWith(github.ref_name, 'fire-') && github.event.repository.default_branch || github.ref_name }}
        run: bash .github/workflows/commit-state.sh "$TARGET" "summary"
