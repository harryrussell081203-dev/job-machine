# Profile layer and wizard
PyYAML>=6.0
requests>=2.28

# The app
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.1
httpx>=0.26

# Only needed when DATABASE_URL points at Postgres (Supabase). The app runs on
# SQLite without it.
psycopg[binary]>=3.1
